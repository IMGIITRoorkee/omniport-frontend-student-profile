import React from "react";
import { Placeholder, Grid, Container, Segment } from "semantic-ui-react";

export const SegmentPlaceholder = () => {
  return <Placeholder as={Segment} style={{ marginTop: "1.5em" }} />;
};
